import styled from "@emotion/styled";

const styles = (theme) => ({
  root: {
    display: "flex",
    heigth: "100%",
  },
  toolbar: {
    height: "70px",
  },
  content: {
    flexGrow: 1,
    padding: "2em",
  },
});

export default styles;
